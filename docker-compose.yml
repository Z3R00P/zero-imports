services:
  db:
    image: mysql:8.0
    container_name: mysql_zeroimports
    restart: always
    environment:
      MYSQL_DATABASE: zeroimports_db
      MYSQL_ROOT_PASSWORD: admin24
      MYSQL_USER: pedro
      MYSQL_PASSWORD: oliveira2804
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data:
